@model Pharmix.Data.Entities.ViewModels.UserViewModel
@{
    ViewBag.Title = "Customer Register";
}

<hr class="">
<div class="col-sm-12">
    @using (Html.BeginForm("ManageModule", "User", FormMethod.Post, new { @class = "form-horizontal frmManageModule" }))
    {
        <input type="hidden" asp-for="Name" value="@Model.Name" />
        <input type="hidden" asp-for="Id" value="@Model.Id" />
        <div class="form-group">
            <label asp-for="Name" class="control-label col-sm-2 ">Username</label>
            <div class="col-sm-3">
                <input asp-for="Name" class="form-control" disabled>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-sm-2 ">Modules</label>
            <div class="col-sm-4">
                @if (Model.UserModuleViewModelList != null && Model.UserModuleViewModelList.Count > 0)
                {
                    for (int i = 0; i < Model.UserModuleViewModelList.Count; i++)
                    {
                        @Html.HiddenFor(m => m.UserModuleViewModelList[i].ModuleId)
                        <div class="checkbox">
                            <label>
                                @Html.CheckBoxFor(m => m.UserModuleViewModelList[i].IsHaveAccess)
                                @Model.UserModuleViewModelList[i].ModuleName
                            </label>
                        </div>
                    }
                }
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-sm-2  "></label>
            <div class="col-sm-4">
                <button type="submit" class="btn btn-default" id="btnSave">Save</button>
            </div>
        </div>

        <br /><br />

    }
</div>
